name: ACM Paper Summarizer

on:
  schedule:
    # 日本時間 (JST) 平日(月〜金) 朝9:00 に自動実行 (UTC 0:00 = JST 9:00)
    - cron: '0 0 * * 1-5'
  workflow_dispatch:

jobs:
  summarize:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install google-genai requests

      - name: Run Paper Summarizer (Lightweight Fast Mode)
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
        run: |
          python acm_slack_notifier.py

      - name: Commit and push updated history
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add processed_papers.json
          git diff --quiet && git diff --staged --quiet || (git commit -m "Update processed papers history" && git push)
